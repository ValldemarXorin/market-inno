apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-shared-init
data:
  010-create-orderservice.sql: |
    -- Creates the database used by order-service.
    -- Runs once on first initialization of the postgres-db volume.
    CREATE DATABASE orderservice;